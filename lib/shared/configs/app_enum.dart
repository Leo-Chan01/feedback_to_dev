enum DialogActionEnum {
  share,
  copy,
  delete,
  movieCredits,
  reportWrongMovie,
  report,
  cancel,
  submit,
  showAd,
  none,
}

// Define an enum for clarity
enum AutoplayOption { enabled, wifiOnly, disabled }
